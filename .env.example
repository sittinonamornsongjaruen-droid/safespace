AUTH_SECRET=change-this-to-a-long-random-string

GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash

# Optional but recommended on Vercel for persistent users.
# Create a Vercel KV/Upstash Redis store and copy these values.
KV_REST_API_URL=
KV_REST_API_TOKEN=
